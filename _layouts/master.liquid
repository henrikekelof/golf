<!DOCTYPE html>
<html lang="sv">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ title }}</title>
        <meta name="description" content="{{ description }}">
        <meta name="robots" content="noindex,nofollow">
        <link rel="apple-touch-icon" sizes="180x180" href="{{ '/' | url }}img/ico/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ '/' | url }}img/ico/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ '/' | url }}img/ico/favicon-16x16.png">
        <link rel="shortcut icon" href="{{ '/' | url }}img/ico/favicon.ico">
        <link rel="stylesheet" href="{{ '/' | url }}main.css">
    </head>
    <body>
        {{ content }}
    </body>
</html>



